## Basic program that exits with a status code of 1 when any arguments are passed to it.
## Otherwise, exits with status code 0.

type [u8] as str8
alias str8 as str

pub fn main(args [str]) i32
	ret args.length > 0
